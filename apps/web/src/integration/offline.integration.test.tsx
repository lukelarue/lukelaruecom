import { describe, expect, it } from 'vitest';

describe('offline integration (frontend)', () => {
  it('is intentionally empty because chat is disabled when the backend is unavailable', () => {
    expect(true).toBe(true);
  });
});
